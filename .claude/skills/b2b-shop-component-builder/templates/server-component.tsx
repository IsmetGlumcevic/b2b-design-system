// Template za Server Component (default)
// Koristi za statički content bez interaktivnosti

import { UntitledIcon } from '@/lib/icons'

interface ComponentNameProps {
  // Props tipovi
}

export default function ComponentName({ props }: ComponentNameProps) {
  return (
    <div className="...">
      {/* Implementacija */}
    </div>
  )
}
